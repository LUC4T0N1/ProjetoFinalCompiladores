programa

    int x.
    lista<int> a.
	lista<texto> b.
	lista<real> c, d.

	a.adc(1).
	b.adc("1").
	c.adc(1).

	escreva(tamanho(a)).
	x := b.obter(0).
	escreva(x).

	x := a.obter(0).
	escreva(x).

	se (x > 1) entao
	{
	escreva(x).
	}senao{
	escreva("menor").
	}


fimprog.