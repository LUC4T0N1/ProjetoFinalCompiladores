programa

  real a.

  texto  t1, t2.

  escreva ("Digite t2 (texto)").
  leia(t2).
  escreva ("t2: ").
  escreva (t2).

  escreva ("Digite a (real)").
    leia(a).
    escreva ("a: ").
    escreva (f).

  escreva ("Digite t1 (texto)").
  leia(t1).
  escreva ("t1: ").
  escreva (t1).

  enquanto (a>3) fazer{
    escreva ("enquanto").
  }

  se(a>3) entao {
   escreva("oi").
  }senao{
    escreva("tchau").
  }

fimprog.